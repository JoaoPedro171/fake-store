import { Fonte2Directive } from './fonte2.directive';

describe('Fonte2Directive', () => {
  it('should create an instance', () => {
    const directive = new Fonte2Directive();
    expect(directive).toBeTruthy();
  });
});
